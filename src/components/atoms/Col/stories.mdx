import { ArgsTable } from '@storybook/addon-docs';
import { default as StyledCol } from '@/atoms/Col';
import { default as StyledFlex } from '@/atoms/Flex';
import { BorderedCol } from '@/atoms/Flex/stories.mdx';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta
  title="Components/Atoms/Col"
  component={StyledCol}
/>

export const Col = (args) => <BorderedCol {...args} />;

export const Flex = (args) => <StyledFlex {...args} style={{ height: '300px' }} />;

# Col

<Canvas>
  <Story
    name="Col"
    args={{ size: 6 }}
    argTypes={{
      size: {
        control: {
          min: 1,
          max: 12,
          type: 'range',
        },
      },
    }}
  >
    {(args) => (
      <Flex wrap="wrap">
        <Col {...args}>{args.size}/12</Col>
        <Col size={6}>6/12</Col>
      </Flex>
    )}
  </Story>
</Canvas>

<ArgsTable story="Col" />
