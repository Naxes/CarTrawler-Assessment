import { ArgsTable } from '@storybook/addon-docs';
import { default as StyledButton } from '@/atoms/Button';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta
  title="Components/Atoms/Button"
  component={StyledButton}
  argTypes={{
    color: {
      control: {
        type: 'select',
      },
      options: [
        'blue',
        'green',
        'slate',
        'violet',
      ],
    },
    radius: {
      control: {
        type: 'select',
      },
      options: [
        1,
        2,
        3,
        4,
        'pill',
        'round',
      ],
    },
    size: {
      control: {
        type: 'select',
      },
      options: [
        'sm',
        'md',
        'lg',
      ],
    },
  }}
/>

export const Button = (args) => <StyledButton {...args} />

# Button

<Canvas>
  <Story
    name="Button"
    args={{
      radius: 1,
      size: 'md',
      color: 'slate',
      outlined: false,
      disabled: false,
      children: 'Hello World!',
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>


<ArgsTable story="Button" />
