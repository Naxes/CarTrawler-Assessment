import { ArgsTable } from '@storybook/addon-docs';
import { default as StyledBadge } from '@/atoms/Badge';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta
  title="Components/Atoms/Badge"
  component={StyledBadge}
  argTypes={{
    color: {
      control: {
        type: 'select',
      },
      options: [
        'blue',
        'cyan',
        'green',
        'slate',
        'mauve',
        'tomato',
        'violet',
        'indigo',
        'crimson',
      ],
    },
    size: {
      control: {
        type: 'select',
      },
      options: [
        'sm',
        'md',
        'lg',
      ],
    },
  }}
/>

export const Badge = (args) => <StyledBadge {...args} />

# Badge

<Canvas>
  <Story
    name="Badge"
    args={{
      size: 'md',
      color: 'slate',
      children: 'Hello World!',
    }}
  >
    {(args) => <Badge {...args} />}
  </Story>
</Canvas>


<ArgsTable story="Badge" />
